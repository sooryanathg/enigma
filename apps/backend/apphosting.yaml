env:
  - variable: FIREBASE_PROJECT_ID
    value: "enigma-d05b9"
    availability:
      - BUILD
      - RUNTIME
  - variable: SERVICE_ACCOUNT_JSON
    secret: SERVICE_ACCOUNT_JSON
    availability:
      - RUNTIME

scripts:
  buildCommand: pnpm install --frozen-lockfile && pnpm --filter backend build
  runCommand: node apps/backend/dist/src/index.js
